// The routes the invariants run against. When you add a page, add its route
// here, or the invariants stop covering it.
export const ROUTES = ["/", "/login", "/bookings", "/rooms/Study%20Room%201.01", "/readme/"];
